{
  "name": "fast-commerce-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/blog",
    "apps/storefront",
    "packages/nav",
    "packages/ui",
    "packages/ui-patterns"
  ],
  "scripts": {
    "build": "npm run build --workspace=@repo/nav && npm run build --workspaces --if-present",
    "dev": "npm run build --workspace=@repo/nav && npm run dev --workspaces --if-present",
    "lint": "npm run build --workspace=@repo/nav && npm run lint --workspaces --if-present"
  },
  "packageManager": "npm@10.9.2"
}
